import {StyleSheet} from "react-native";

export const globalStyles = StyleSheet.create({
    darkView:{
        flex: 1,
        backgroundColor: 'black'
    },
    darkText: {
        color: 'white',
    }
});